// Placeholder for Qelva relayer node
// Later: consume intents from backend and execute on Solana with split & delay.

export async function startRelayer() {
  console.log('Qelva relayer placeholder booted');
}
